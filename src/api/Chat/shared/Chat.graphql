type Chat {
  id: Int!
  messages: [Message]! #graphql에서 리스트 표현
  passenger: User!
  passengerId: Int!
  driver: User!
  driverId: Int!
  ride: Ride!
  rideId: Int
  createAt: String!
  updateAt: String
}

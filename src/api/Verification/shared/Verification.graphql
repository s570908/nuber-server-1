type Verification {
  id: Int!
  target: String!
  payload: String!
  key: String!
  verified: Boolean!
  createAt: String!
  updateAt: String #updateAt은 없을 수 있기 때문에 필수가 아니다.
}

# v = Verification
# v.target = phone
# v.payload = +24214242142
# v.key = 5535

# hello verify with the number 5535

# 5535

# v(payload: +24214242142, key: 5535)
